whisper_derive::relation! {
    fn is_sugar();

    garply X if
        grault X,
        try X = hork (zuul waldo) xyzzy;
}

whisper_derive::relation! {
    fn is_anywhere();

    garply (try Y = zed);
}
